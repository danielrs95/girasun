<h1>Books#index</h1>
<h1>Biblioteca</h1>

<%= link_to 'Añadir nuevo libro', new_book_path %>

<table>
  <thead>
    <tr>
      <th>Título</th>
      <th>Autor</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td>
          <%= link_to 'Ver', book_path(book) %>
          <%= link_to 'Editar', edit_book_path(book) %>
          <%= link_to 'Eliminar', book_path(book), method: :delete, data: { confirm: '¿Estás seguro?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
